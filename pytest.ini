[pytest]
testpaths = tests
addopts = -m "not slow"
markers =
    slow: makes several live model calls; excluded by default.
        These are real tests, not optional ones — but each is multiple round trips to a
        rate-limited endpoint, so a default run that includes them stalls unpredictably
        and tells you nothing about your change. Run explicitly with `-m slow`.
